#include<stdio.h>

int main() {
    float x,p;
    float y;
    scanf("%f %f",&x,&p);
    int sum=0;
    while((int)x>0){
        sum=sum+x;
        y=(x*(p)/100.0);
        x=x-y;
        x=(int)x;
    }
    printf("%d",sum);
}
